[project]
name = "velox-server"
version = "0.0.1"
description = "Always-on runtime for VELOX."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "httpx",
    "pydantic>=2",
    "pytest",
    "uvicorn[standard]",
]

[tool.setuptools]
packages = []
